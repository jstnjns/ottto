import _ from 'lodash'

import React from 'react'
import { connect } from 'react-redux'
import { Actions } from 'react-native-router-flux'

import Action from 'rules/condition-component'


export default connect(
  // mapStateToProps
  (state) => ({

  }),
  // mapDispatchToProps
  (dispatch) => ({

  })
)(Action)
